

import 'package:flutter/material.dart';

class car_mobile extends StatelessWidget {
  const car_mobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('asdfasdfaf'),
        ),
      ),
    );
  }
}